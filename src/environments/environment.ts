export const environment = {
  production: false,
  /**
   * Local chat-backend URL. The backend runs on the user's machine and uses
   * Claude Code CLI OAuth — no API key. Override per-deployment if needed.
   * See the secondbrain repo's chat-backend/ folder for the server.
   */
  chatBackendUrl: 'http://localhost:8787'
};
